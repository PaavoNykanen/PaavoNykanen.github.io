import { Flex, Typography } from 'antd';

const PersonalInfo = () => {
  return (
    <Flex style={{ margin: '10px' }}>
      <Typography>
        Meitsi
      </Typography>
    </Flex>
  );
};

export default PersonalInfo;
